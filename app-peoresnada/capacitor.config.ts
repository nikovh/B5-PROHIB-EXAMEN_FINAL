import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'peoresnada.app',
  appName: 'peoresnada-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
